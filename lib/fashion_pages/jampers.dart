import 'package:flutter/material.dart';

class Jampers extends StatelessWidget {
  const Jampers({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}